"""Weekly digest scheduler entry point. Phase 8."""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


async def run_weekly_digest_job() -> None:
    """Scheduler entry point. Stub until Phase 8."""
    raise NotImplementedError("digest.worker.run_weekly_digest_job (Phase 8)")
